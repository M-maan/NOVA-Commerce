'use client';
import { useEffect, useState } from 'react';
import { api } from '@/lib/api/client';
type Refund = { id: string; orderId: string; amount: string; reason: string; status: string; createdAt: string };
export default function AdminRefundsPage() { const [items, setItems] = useState<Refund[]>([]); const [error, setError] = useState(''); useEffect(() => { api.get<Refund[]>('/admin/refunds').then(setItems).catch((e) => setError(e instanceof Error ? e.message : 'Unable to load refunds.')); }, []); return <main className="mx-auto max-w-5xl space-y-6 p-6"><h1 className="text-3xl font-bold">Refunds</h1>{error && <p role="alert" className="text-red-500">{error}</p>}<div className="overflow-x-auto rounded border"><table className="w-full text-left text-sm"><thead className="bg-muted"><tr><th className="p-3">Order</th><th className="p-3">Amount</th><th className="p-3">Status</th><th className="p-3">Reason</th></tr></thead><tbody>{items.map((item) => <tr key={item.id} className="border-t"><td className="p-3">{item.orderId}</td><td className="p-3">{item.amount}</td><td className="p-3">{item.status}</td><td className="p-3">{item.reason}</td></tr>)}</tbody></table></div></main>; }
