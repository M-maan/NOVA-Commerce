import { Headphones, ShieldCheck, Truck } from 'lucide-react';
export function TrustSection() { return <section className="grid gap-4 sm:grid-cols-3">{[[Truck,'Fast, tracked delivery','Clear shipping updates from checkout to doorstep.'],[ShieldCheck,'Secure by design','Protected payments and privacy-first account controls.'],[Headphones,'Human support','A considered experience when you need a hand.']].map(([Icon,title,copy]) => { const Feature=Icon as typeof Truck; return <article key={String(title)} className="rounded-2xl border bg-card p-6"><Feature className="size-5 text-primary" /><h2 className="mt-4 font-semibold">{String(title)}</h2><p className="mt-2 text-sm text-muted-foreground">{String(copy)}</p></article>; })}</section>; }
